apiVersion: v1
kind: Service
metadata:
  name: taxlens-redis
  namespace: taxlens-db
spec:
  type: ClusterIP
  selector:
    app: taxlens-redis
  ports:
    - port: 6379
      targetPort: 6379
      name: redis
