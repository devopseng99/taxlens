apiVersion: v1
kind: Service
metadata:
  name: taxlens-ui
  labels:
    app.kubernetes.io/name: taxlens
    app.kubernetes.io/component: ui
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: taxlens
    app.kubernetes.io/component: ui
